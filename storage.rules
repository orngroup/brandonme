rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Marketing library files — any signed-in staff member can read/write.
    match /marketing/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    // Deny everything else.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
